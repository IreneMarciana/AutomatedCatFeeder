target Python{
    keepalive: true
}

main reactor { 
    timer t(1 sec, 1 sec);
    output weight: double;

    reaction(startup) {=
	    from HX711 import SimpleHX711, Rate
	    self.hx = SimpleHX711(5, 6, 1, 0, Rate.HZ_80)
    =}

    reaction(t) {=
	    print(hx.weight())
    =}
}