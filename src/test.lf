target C {
  platform: {
    name: "linux"
  },
  single-threaded: true,
  build-type: "release"
}

main reactor {
    timer t(0, 1 sec);
    reaction(t) {=
        printf("Hello from Raspberry Pi 3!\n");
    =}
}
